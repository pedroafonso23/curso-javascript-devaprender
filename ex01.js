// Escreva uma function que usa 2 numeros e retorna o maior deles

let valorMaior = max(5, 9)
console.log(valorMaior)

function max(num1, num2) {
    // if(num1 > num2) return num1
    return num1 > num2 ? num1 : num2
}